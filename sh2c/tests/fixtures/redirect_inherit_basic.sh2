func main() {
    with redirect {
        stdout: [file("out.txt"), inherit_stdout()]
        stderr: [file("err.txt"), inherit_stderr()]
    } {
        print("stdout line")
        print_err("stderr line")
        let res = try_run("sh", "-c", "exit 7")
        print("status=" & res.status)
    }
}
