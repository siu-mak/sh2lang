#!/usr/bin/env bash
main() {
  if [ -n ' ' ]; then
    printf '%s\n' 'T_SPACE'
  else
    printf '%s\n' 'F_SPACE'
  fi
  if [ -n '
' ]; then
    printf '%s\n' 'T_NEWLINE'
  else
    printf '%s\n' 'F_NEWLINE'
  fi
}
__sh2_status=0
main "$@"
