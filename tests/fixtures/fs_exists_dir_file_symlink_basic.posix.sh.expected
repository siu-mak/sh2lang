main() {
  __sh2_loc="/srv/repos/rust/sh2c/tests/fixtures/fs_exists_dir_file_symlink_basic.sh2:2"
  'sh' '-c' 'rm -rf tmp; mkdir -p tmp/dir; : > tmp/file; ln -s file tmp/link'; __sh2_status=$?; if [ $__sh2_status -ne 0 ]; then printf 'Error in %s\n' "$__sh2_loc" >&2; exit $__sh2_status; fi
  if [ -e 'tmp/file' ] && [ -f 'tmp/file' ] && [ -d 'tmp/dir' ] && [ -L 'tmp/link' ]; then
    printf '%s\n' 'ok'
  else
    printf '%s\n' 'bad'
  fi
}
__sh2_status=0
main "$@"
