#!/usr/bin/env bash
__sh2_check() { local s="$1"; local loc="$2"; if (( s != 0 )); then if [[ -n "$loc" ]]; then printf 'Error in %s\n' "$loc" >&2; fi; exit "$s"; fi; }
main() {
  if ! [ -n 'x' ]; then
    printf '%s\n' 'T_NOT_X'
  else
    printf '%s\n' 'F_NOT_X'
  fi
  if ! [ -n '' ]; then
    printf '%s\n' 'T_NOT_EMPTY'
  else
    printf '%s\n' 'F_NOT_EMPTY'
  fi
}
__sh2_status=0
main "$@"
