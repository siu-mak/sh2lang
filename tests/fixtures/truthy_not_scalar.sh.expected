#!/usr/bin/env bash
main() {
  if ! [ -n 'x' ]; then
    printf '%s\n' 'T_NOT_X'
  else
    printf '%s\n' 'F_NOT_X'
  fi
  if ! [ -n '' ]; then
    printf '%s\n' 'T_NOT_EMPTY'
  else
    printf '%s\n' 'F_NOT_EMPTY'
  fi
}
__sh2_status=0
main "$@"
