__sh2_which() { command -v -- "$1" 2>/dev/null || true; }
main() {
  __sh2_loc="tests/fixtures/which_found.sh2:3"
  p="$( __sh2_which 'sh' )"
  if [ "$p" != '' ]; then
    printf '%s\n' 'OK'
  else
    printf '%s\n' 'NO'
  fi
}
__sh2_status=0
main "$@"
