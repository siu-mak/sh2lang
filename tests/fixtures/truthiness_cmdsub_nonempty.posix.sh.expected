#!/bin/sh
main() {
  __sh2_loc="tests/fixtures/truthiness_cmdsub_nonempty.sh2:2:3"
  out="$( 'sh' '-c' 'printf '\''%s'\'' hi' )"
  __sh2_status=$?; (exit $__sh2_status)
  if [ -n "$out" ]; then
    printf '%s\n' 'ok'
  else
    printf '%s\n' 'bad'
  fi
}
__sh2_status=0
main "$@"
