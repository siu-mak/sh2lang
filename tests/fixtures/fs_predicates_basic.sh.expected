main() {
  "sh" "-c" "rm -rf tfile tlink; echo hi > tfile; chmod 600 tfile; chmod +x tfile; ln -s tfile tlink"
  if [ -L "tlink" ] && [ -x "tfile" ] && [ -r "tfile" ] && [ -w "tfile" ]; then
    echo "ok"
  else
    echo "bad"
  fi
}

main "$@"
