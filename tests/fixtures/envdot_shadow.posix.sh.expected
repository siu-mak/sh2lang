shadow() {
  FOO="${1}"
  printf '%s\n' "${FOO-}"
  printf '%s\n' "$FOO"
}

main() {
  (
    export FOO='outer'
    shadow 'local'; __sh2_status=$?; (exit $__sh2_status)
  )
}
__sh2_status=0
main "$@"
