#!/bin/sh
main() {
  __sh2_loc="tests/fixtures/compare.sh2:2:5"
  user='admin'
  __sh2_status=$?; (exit $__sh2_status)
  if [ "$user" = 'admin' ]; then
    printf '%s\n' 'welcome root'
  fi
  if [ "$user" != 'guest' ]; then
    printf '%s\n' 'not guest'
  fi
}
__sh2_status=0
main "$@"
