__sh2_err_handler() { printf "Error in %s\n" "${__sh2_loc:-unknown}" >&2; }
trap '__sh2_err_handler' ERR
main() {
  __sh2_loc="tests/fixtures/map_duplicate_keys.sh2:2"
  local -A m=( ['a']='1' ['a']='2' ['b']='3' )
  local -a __sh2_keys_m=( 'a' 'b' )
  for __sh2_k in "${__sh2_keys_m[@]}"; do
    local k="$__sh2_k"
    local v="${m[$__sh2_k]}"
    printf '%s\n' "$k"
    printf '%s\n' "$v"
  done
  printf '%s\n' "${m['a']}"
}
__sh2_status=0
main "$@"
