main() {
  'sh' '-c' 'rm -rf tdir tfile; mkdir tdir; echo hi > tfile'
  if [ -e 'tfile' ] && [ -f 'tfile' ] && [ -d 'tdir' ]; then
    printf '%s\n' 'ok'
  else
    printf '%s\n' 'bad'
  fi
}

main "$@"

