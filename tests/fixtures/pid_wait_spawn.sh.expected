main() {
  ( 'sh' '-c' 'exit 0' ) &
  p="$!"
  wait "$p"; __sh2_status=$?
  if [ "$__sh2_status" = "0" ]; then
    printf '%s\n' 'wait_ok'
  else
    printf '%s\n' 'wait_bad'
  fi
}

__sh2_status=0
main "$@"
