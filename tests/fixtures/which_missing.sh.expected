#!/usr/bin/env bash
__sh2_check() { local s="$1"; local loc="$2"; if (( s != 0 )); then if [[ -n "$loc" ]]; then printf 'Error in %s\n' "$loc" >&2; fi; exit "$s"; fi; }
__sh2_which() { command -v -- "$1" 2>/dev/null || true; }
main() {
  printf '%s\n' "$( __sh2_which 'definitely_not_a_real_cmd_123' )"
}
__sh2_status=0
main "$@"
