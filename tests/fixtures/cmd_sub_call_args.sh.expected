__sh2_err_handler() { local s=$?; if [[ "${BASH_COMMAND}" == *"(exit "* ]]; then return $s; fi; printf "Error in %s\n" "${__sh2_loc:-unknown}" >&2; return $s; }
set -o errtrace
trap '__sh2_err_handler' ERR
show() {
  local __sh2_loc=""
  echo $@
}

main() {
  local __sh2_loc=""
  __sh2_loc="tests/fixtures/cmd_sub_call_args.sh2:6:3"
  out="$( 'show' "$@" )"
  printf '%s\n' "$out"
}
__sh2_status=0
main "$@"
