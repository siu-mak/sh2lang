__sh2_err_handler() { printf "Error in %s\n" "${__sh2_loc:-unknown}" >&2; }
trap '__sh2_err_handler' ERR
main() {
  __sh2_loc="/srv/repos/rust/sh2c/tests/fixtures/fs_readable_writable_basic.sh2:2"
  'sh' '-c' 'rm -f ro rw; : > ro; : > rw; chmod 444 ro; chmod 644 rw'; __sh2_status=$?; (exit $__sh2_status)
  if [ -r 'ro' ] && ! [ -w 'ro' ] && [ -r 'rw' ] && [ -w 'rw' ]; then
    printf '%s\n' 'ok'
  else
    printf '%s\n' 'bad'
  fi
}
__sh2_status=0
main "$@"
