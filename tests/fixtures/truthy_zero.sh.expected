#!/usr/bin/env bash
main() {
  if [ -n '0' ]; then
    printf '%s\n' 'T_STR_0'
  else
    printf '%s\n' 'F_STR_0'
  fi
  if [ -n "0" ]; then
    printf '%s\n' 'T_NUM_0'
  else
    printf '%s\n' 'F_NUM_0'
  fi
}
__sh2_status=0
main "$@"
