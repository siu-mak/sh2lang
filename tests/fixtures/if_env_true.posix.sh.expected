#!/bin/sh
__sh2_check() { s="$1"; loc="$2"; if [ "$s" -ne 0 ]; then if [ -n "$loc" ]; then printf 'Error in %s\n' "$loc" >&2; fi; exit "$s"; fi; }
main() {
  if [ -n "$TESTVAR" ]; then
    printf '%s\n' 'yes'
  fi
}
__sh2_status=0
main "$@"
