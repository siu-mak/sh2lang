#!/usr/bin/env bash
main() {
  if true; then
    printf '%s\n' 't_ok'
  else
    printf '%s\n' 't_bad'
  fi
  if false; then
    printf '%s\n' 'f_bad'
  else
    printf '%s\n' 'f_ok'
  fi
}
__sh2_status=0
main "$@"
