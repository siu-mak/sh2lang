__sh2_err_handler() { local s=$?; if [[ "${BASH_COMMAND}" == *"(exit "* ]]; then return $s; fi; printf "Error in %s\n" "${__sh2_loc:-unknown}" >&2; return $s; }
set -o errtrace
trap '__sh2_err_handler' ERR
main() {
  local __sh2_loc=""
  __sh2_loc="tests/fixtures/list_index_numeric.sh2:2:1"
  xs=('a' 'b')
  printf '%s\n' "${xs[0]}"
  printf '%s\n' "${xs[1]}"
}
__sh2_status=0
main "$@"
