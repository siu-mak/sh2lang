#!/bin/sh
__sh2_check() { s="$1"; loc="$2"; if [ "$s" -ne 0 ]; then if [ -n "$loc" ]; then printf 'Error in %s\n' "$loc" >&2; fi; exit "$s"; fi; }
main() {
  __sh2_loc="tests/fixtures/len_basic.sh2:2:3"
  s='hello'
  __sh2_status=$?
  __sh2_check "$__sh2_status" "${__sh2_loc:-}"
  printf '%s\n' "$( printf "%s" "$s" | awk 'BEGIN{l=0} {l=length($0)} END{print l}' )"
}
__sh2_status=0
main "$@"
