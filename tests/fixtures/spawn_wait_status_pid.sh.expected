main() {
  "sh" "-c" "exit 7" &
  if [ "$!" -gt "0" ]; then
    echo "pid_ok"
  else
    echo "pid_bad"
  fi
  wait "$!"
  if [ "$?" = "7" ]; then
    echo "status_ok"
  else
    echo "status_bad"
  fi
}

main "$@"
