#!/bin/sh
__sh2_check() { s="$1"; loc="$2"; if [ "$s" -ne 0 ]; then if [ -n "$loc" ]; then printf 'Error in %s\n' "$loc" >&2; fi; exit "$s"; fi; }
main() {
  printf '%s\n' 'before'
  __sh2_loc="tests/fixtures/exec_replace.sh2:3:3"
  exec 'sh' '-c' 'echo after'
  printf '%s\n' 'never'
}
__sh2_status=0
main "$@"
